#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    string inputStr;
    getline(cin, inputStr);

    for (size_t i = 0; i < inputStr.length(); i++)
    {
        if (inputStr[i] == " ")
        {
            
        }
    }

    return 0;
}